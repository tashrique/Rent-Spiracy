# Environment configuration
ENVIRONMENT=development # Set to "production" for production deployment

# API Configuration
PORT=8000
CORS_ORIGINS=https://rent-spiracy.vercel.app,https://rentspiracy.tech,https://www.rentspiracy.tech,http://localhost:3000

# Optional: API keys for services (when implemented)
# GEMINI_API_KEY=your_gemini_api_key
# ELEVENLABS_API_KEY=your_elevenlabs_api_key

# MongoDB Database Connection
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
DATABASE_NAME=rent_spiracy

# Frontend URL configuration
NEXT_PUBLIC_API_URL=http://localhost:8000  # or your deployed backend URL

# Google Gemini API Integration
# Get your API key from: https://aistudio.google.com/app/apikey
# Required for lease analysis functionality
GEMINI_API_KEY=your_gemini_api_key_here

# Production settings
# LOG_LEVEL=INFO # Set to ERROR in production to reduce log noise

# Domain configuration (only needed if different from the API's own domain)
# FRONTEND_DOMAIN=https://rentspiracy.tech
